// Site configuration. Safe to commit: a Google Maps browser key must be restricted to this site's address in the Google Cloud console.
window.TRIP_CONFIG = {
  // Paste a Google Maps JavaScript API key here to switch the map to Google Maps (with Google road routing).
  // Leave empty to use the keyless OpenStreetMap map with OSRM road routing.
  googleMapsKey: '',
  // Optional: a Map ID from the Google Cloud console for a custom map style.
  googleMapId: ''
};
