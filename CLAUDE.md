# CLAUDE.md — SPARK repo root

@SPARK_Site/CLAUDE.md

This repo is the **SPARK artifact set** handed over from a Claude Cowork session (last state: v3.1, verified green, 30 Jul 2026). The imported file above is the living project memory — narrative rule, verified GCP facts, settled decisions, open threads. Read `SPARK_Context_Handoff.md` once, at the start of your first session, for the full story.

## Layout

- `SPARK_Site/` — the product: 9 self-contained HTML files (open `SPARK_Hub.html`) + project memory. Edit these in place; never split assets out. (`SPARK_Portal.html` and `SPARK_Leadership_Hub.html` were retired and deleted 1 Aug 2026, recoverable from git history.)
- **Global writing rule: never use the em dash "—" in anything produced for this project** (artifacts, emails, UI strings, commits, PRs). See the rule block at the top of `SPARK_Site/CLAUDE.md`.
- **Delivery rule (Majid, 2 Aug 2026): when a deliverable is finished, always attach the output file(s) directly in the chat AND give the GitHub raw link.** Both, every time, without being asked.
- **Versioning rule (Majid, 3 Aug 2026): every prompt that changes a deliverable bumps its version, starting at v1.** The version is stamped in the artifact itself (hero chip + footer line) and in the delivered filename (`FinOps_Dashboard_v1.html`, then v2, v3...). The stable `FinOps_Dashboard.html` name is kept as well, since SharePoint publishing overwrites the same file.
- `finops/` — the standalone Cloud FinOps dashboard stream (SharePoint-hosted, zero JS, NOT linked to the SPARK set). See the FinOps section below.
- `comms/` — the launch announcements (three phases × AR/EN, copy-to-clipboard, `[PORTAL-LINK]` placeholder).
- `spark-identity/` — the official identity package (logos, fonts, tokens, BRAND.md, Solids v2 components). Source of truth for the customer portal and anything built from now on.
- `tools/verify.js` — Playwright verifier (`--demo --counters --form --chat --lang`). **Every HTML change must pass it before you declare done.** If Playwright's own Chromium is unavailable, set `CHROMIUM_PATH` to a system Chromium.
- `SPARK_Context_Handoff.md` — session history, people, design contract, why each decision was made.
- `shots/` — created by the verifier; screenshots to actually look at.

## Quickstart

```bash
npm install && npx playwright install chromium   # once
npm run verify -- SPARK_Site/<file>.html         # after every change
```

## Definition of done (non-negotiable)

1. `verify.js` reports: `jsErrors: []`, `svgIssues: []`, all `.rv` revealed, all `.st` staged in, `embedded.cls: true`, `useMissing: []`.
2. You have opened and *looked at* the screenshots in `shots/` — the verifier catches geometry, not ugliness.
3. Cross-file consistency held: a fact changed in one file (project IDs, budget thresholds, narrative lines) is changed in every file that states it. `grep -l` across `SPARK_Site/*.html` before closing.
4. `SPARK_Site/CLAUDE.md` updated if a decision was made or a fact changed.

Known trap already fixed once: a broad `.figbox svg { width:100% }` rule blows up nested icon SVGs — keep such rules scoped to direct children.

## FinOps dashboard stream (`finops/`, standalone)

- Recurring artifact for IT leadership, hosted on SharePoint: one self-contained HTML file, zero JavaScript, timestamped, updated monthly. Not part of the SPARK Hub; it only borrows the DE identity (fonts, orb, palette).
- v1.1 (Q2 2026, all platforms) shipped 2 Aug 2026 with adversarial-review fixes. Superseded in direction by the client review the same day; keep the file as the Q2 archive.
- **v2 direction (client review via Majid, 2 Aug 2026):** keep it simple, short and sweet; **GCP spend ONLY** (drop Azure, Oracle, O365, SaaS budget sections); exactly **two sections: GCP spend and Sandbox spend**. Sandbox quarterly narrative: usage cost ran very high, the planning team restructured the sandbox, cost is now at a sustainable and stable level.
- **Period switch replaces the hero chip row** (Majid wants it prominent): Month and Contract-to-date always; **Quarter appears only in quarter-end months**. Implement as CSS-only radio tabs (zero JS holds, SharePoint-safe).
- "To date" means the contractual period. NOTE: the billing account (`cntxt-...-002`) only has data from **1 Oct 2025**; the contract start is 1 Jun 2025. Jun to Sep 2025 spend, if any, sits outside this account; the to-date view is labelled with its true range. Open question for Majid: whether an earlier account (-001) holds pre-Oct spend.
- **v2 SHIPPED 2 Aug 2026, operating model settled:** `finops/FinOps_Builder.html` is Majid's private local operator tool (JS allowed locally; never published). He drops the six Reports CSVs in (Month/Quarter/To-date, all-GCP + sandbox-filtered), edits the credits note and narratives, clicks Generate; it downloads **`FinOps_Dashboard.html`** (zero JS, self-contained, radio-tab period switch). He uploads that to SharePoint OVERWRITING the same file, so the public link never changes. First edition: July 2026 + H1 2026 + to-date, built from Majid's 2 Aug extracts (H1 net $614,875 matched the quarterly report to the cent).
- **Chart decision (Majid, 2 Aug 2026, chose from six rendered samples):** each section leads with a **donut + center KPI** (top 4 services + Other, on-slice percents, Lafet center figure, legend with share bars; "trendy and professional"), followed by an editable **key highlight** box (auto = fastest-growing service among those >= 3% of period net; concentration fact as fallback), and an expandable **details** block (ranked bars with gross ghost + full data table) via native details/summary, zero JS. "Simple but expandable" is the standing design goal.
- **v2.2 leadership polish (Majid, 2 Aug 2026):** sticky **liquid-glass top bar** (backdrop-filter, carries the brand plus the period tabs, CSS-only); SPARK **blade glyph** replaces the rule before each hero figure; hero carries a breathing **DE solid** (CSS 3D-look SVG, frozen under reduced motion); every sandbox section ends with a single **"The sandbox becomes SPARK"** panel = short narrative + the fixed journey animation (**Application > ServiceNow > approved > workspace > shared engine**, with the approval line firing on the same beat as the green check) + the three release phases (ITDT, all MoEnergy, energy ecosystem) + the department credit. Footer eyebrow is now **"Prepared for the eyes of"** and the credit line is the **IT Strategy & Planning Department** (never a personal name). The department is named in the hero, the glass bar, the chips, the SPARK panel and the footer. USD is explained in the hero (the console meters and invoices in USD, no conversion).
- **v2.3 showcase build (Majid, 2 Aug 2026):** fixed **liquid-glass top bar** (brand + DE wordmark, "GCP SPEND REPORT · IT STRATEGY & PLANNING", period tabs, animated **EN/AR switch** at the far right, bar stays LTR in both languages). **Bilingual, zero JS**: every string ships EN + AR twins toggled by a checkbox, `dir` flips to RTL, charts and tables stay LTR, figures wrapped in `bdi` so `$1,234` never reverses; the builder carries optional Arabic fields (credits note + three narratives) that fall back to English when empty. **Journey figure rebuilt**: two labelled boundaries (MINISTRY BOUNDARY · RIYADH DC for application/ServiceNow/**FinOps team** approval, MINISTRY BOUNDARY · GCP DAMMAM DC · SPARK FOLDER for workspace/engine), application shows the requested fields (data classification, business value, monthly budget), three **sequential provisioning rings** (provisioning > spend limits and killswitch > QA and handover, each filling then turning green before the next appears), **end users working inside the workspace**, and the sovereignty line "data never leaves the Ministry boundary". Sandbox card now opens with bold "The sandbox story:" and holds narrative + figure + phases; the department footnote was removed. Blade glyph is **sharp-edged with a travelling light shine**; "View the detail" is a bordered button (45+ legibility); the USD note is italic. Hero carries the four **official motif shapes as liquid-glass ornaments** (masked sheen, staggered float, right-side only, hidden on small screens) and the DE wordmark **writes on with a light flare**, mirroring the brand motion video.
- **v3 (3 Aug 2026), the QA and hardening pass.** Everything below is now true of `finops/FinOps_Dashboard.html`:
  - **The embedded SPARK animation is plain static HTML, not a bundle.** Both animation files Majid supplied were *bundler dev harnesses*: a JS unpacker, a `<noscript>` "This page requires JavaScript to display" notice, an "Unpacking..." chip and a red `[bundle] ...` error overlay, with the real figure and ten font files held in a JSON manifest. On SharePoint with scripts blocked, leadership would have seen a navy placeholder square. The pipeline that fixes it lives in the scratchpad (`step1_fonts.py` > `step2_bake.js` > `step3_patch.py`): lift the template, swap the ten bundled faces for the dashboard's own three IBM Plex woff2 (subset to ASCII, about 18 KB each), bake the JS-computed timing (`stretch()`, 1.6x) and return-line geometry into inline styles, drop the script, and replace the `.go` gate so the CSS animations simply run. **Keep `:root{--font-body:...}` when replacing the @font-face span** or the whole figure falls back to Times New Roman.
  - **The figure fits itself with a media-query ladder** (10px bands, scale = W/980) and the parent frame only carries `aspect-ratio:980/334`. No JS, no dead space, no clipping, at every width.
  - **Wording guard applied inside the animation:** "Ministry-tuned to ministry wording and design" became "Approved to Ministry requirements". Models are approved to Ministry requirements, never tuned to or trained on them.
  - **Hero floating objects are the `de-motifs` liquid-glass layer** (handoff `Floating_Background_Objects_Uplift.zip`): pure CSS, no images, backdrop-filter glass, one light position. All eight legacy `.lgs`/`.lgo` override blocks and the four DE_icons PNGs are deleted. Its positions are solved against a **1440x370 hero with real lockups**, so the objects render only at **1280px and up**; below that the copy column widens, the brand row re-flows and in RTL the period strip swaps side, and no single placement clears the copy in both languages. The depth wash, refraction field and key light stay at every width. Re-measure the hero ink boxes before widening that range.
  - **Quarter period is H1 2026 everywhere.** It used to be labelled "Q2 to Q3 2026" while carrying the verified H1 net of $614,875 (and a Q2 to Q3 window would include months with no data). Labels, donut centre, key highlight, chips and the mobile period row all say H1 2026 now.
  - **Sandbox key highlight, Majid's approved storyline (3 Aug):** the sandbox has matured into a fully governed platform; with financial controls and automated provisioning in place it is being relaunched as SPARK, providing secure and scalable access to cloud innovation. SPARK stays the hidden message. The quarter and to-date views carry the same spine with their own period fact in front.
  - **Print and PDF now carry the whole report:** the period is stated in words (`.print-period::after`, EN and AR), the tab strip and dropdowns are hidden, and every `details` block opens through `::details-content` so the ranked bars and data tables survive the export.
  - **Size: 6.75 MB to 1.46 MB.** The DE logo APNG was 640x344 for a 188x64 box (1.73 MB of base64); re-encoded at 376x202 it is 571 KB with identical playback (3.55 s, 71 frames merged to 49 with durations summed). The animation's fonts were 227 KB per copy, three copies; ASCII-subset with layout tables kept, they are 55 KB per copy. **Subset by the full ASCII set, not by the characters in the source**: `text-transform:uppercase` means the uppercase forms never appear in the markup, and a source-derived subset silently drops E, T, H, I, V and others.
  - **Delivery channels, honestly:** SharePoint yes (self-contained, zero JS, no external requests). PDF yes, with the print path above. **Email body no**: Outlook renders with Word (no flex, grid, custom properties, backdrop-filter, iframes or details) and Gmail strips `<head>`; send it as an attached .html or an attached PDF instead.
- **v4 (4 Aug 2026), the period control and summary balance pass:**
  - **The two headline figures are centred in their halves**, so the pair reads symmetrically about the blade instead of hugging the outer edges of the card. The note line's `max-width` went to 58ch so the sandbox caption stays on one line.
  - **The month and quarter dropdowns moved inside the hero period buttons.** Each of Month and Quarter is now a `.seg`: the label body switches the view, and a caret button at its inline end opens that period's list. The standalone dropdown row under the hero is gone; only the "data not available" banner stays there. Still zero JS: `details`/`summary` for the panel, hidden radios for the choice, and the button label reads back the selection through the same `.opt` show/hide rules.
  - **Two collisions to remember when touching this control:** the page ships a blanket `details summary{...!important}` rule for its View-details pills, so every property on the caret button has to be `!important` to reach it; and the menu options are `<label>` elements inside `.sw`, so they inherit the segment-button styles unless explicitly reset. Both are handled in the late `#v4-period-and-summary` style block, which must stay last so it wins.
  - **The hero no longer clips.** `.hero` is `overflow:visible` and the two glow washes moved into their own `.hero-fx` clip box, so the menu can open past the hero edge; `.hero .sw` carries `z-index:20` so the open menu is not covered by the cards below. On phones the menu opens from the caret's start edge (a segment is only a third of the screen).
  - **The mobile period value row is a real element now** (`.sw-value` with `.svp` period twins) rather than `.sw::after` with `content`, which removes the last `html.is-ar` JS dependency from the period control.
- **v5 (4 Aug 2026), one menu at a time.** Two period menus could be open together, and over a section they did not describe. Fixed with three CSS mechanisms, no JS:
  - both menus share `name="periodmenu"`, so the browser's exclusive-accordion behaviour closes one when the other opens;
  - **the caret belongs to whichever period is active** (`.sw .seg .dd{display:none}` plus a `#pv-X:checked` rule per segment), so switching section hides the caret and its list in the same stroke and a menu can never hang over the wrong section;
  - a third member of the same group, `.dd-dismiss`, is a fixed full-page scrim that a click anywhere off the strip lands on, closing the menu. **Its `pointer-events` must be gated on a menu that is actually on screen** (`body:has(#pv-m:checked):has(.dd-mo[open])`, and the quarter twin), otherwise a details left open behind a hidden caret leaves an invisible scrim swallowing every page click.
  - The scrim has to live at body level: `.sw` carries `backdrop-filter`, which makes it the containing block for `position:fixed` descendants, so a scrim inside the strip would only cover the strip.
  - With scripts available, a period change also closes the menus outright; with scripts blocked it stays open behind the hidden caret and reappears if you return to that section, which is cosmetic only and one outside click clears it.
- **v6 (4 Aug 2026), Majid's review round:**
  - **Phase 1 now reads as a soft launch that is not fully launched yet.** The chip is "In progress" (was "Current"), the label is "Phase 1 · ITDT · soft launch", the description is "Not fully launched yet: a limited pilot with a first group of users while the service is still being built out", and the intro line reads "Release is phased, and phase 1 has not fully launched yet". EN and AR, all three period views. The chip also needed its pill styling restored in the late block: a later rule had stripped its background, radius and weight so it was rendering as plain body text.
  - **The $1,000 Gen App Builder credit is removed from the artifact entirely**, not just from the credit-sources panel: the row is gone from all three views and the credits note in every period now reads "No credits remain available...". Note this contradicts the 2 Aug credits fact below, which recorded the trial credit as still open; Majid's instruction is to stop referencing it anywhere, so the dashboard no longer states it either way.
  - **The credit meter sits higher and reads heavier**: the wrapper lost its `margin-top:auto` push to the card floor (now a fixed 20px above), the track went 6px to 14px, and the caption went 10px/500 to 11.5px/600.
- **v7 (4 Aug 2026), every period carries its own numbers.** Majid re-exported Jan to Jun 2026 with the discount options on (the six months now reconcile to the H1 file to within two cents, GCP and sandbox), so the seven "data not available" banners are gone along with their CSS.
  - The month view holds seven period blocks and the quarter view three, each wrapped in `.mv .mv-<key>` and revealed by `#m-<key>:checked~main .v-m .mv-<key>`. The credit card, the sandbox card shell and the whole SPARK panel with its animation stay **shared**, outside the swap: duplicating the panel per period would have added 92 KB of animation seven times over. File went 1.47 MB to 1.59 MB.
  - **Q1 and Q2 are summed from the monthly CSVs** (service-level sums are additive and Q1 + Q2 reproduces H1 exactly). Per-service change for Q2 is computed against Q1; **Q1 shows `n/a`** because the account holds no Q4 2025 per-service detail.
  - The generator is `scratchpad/finops/gen.py` plus `blocks.py` and `install.py`. Donut geometry was reverse-engineered from the published July view and reproduces it to sub-pixel: r=118, stroke 28, centre (180,150), a 2.4px arc gap at each slice end, on-slice percent only at 15% share or more, legend rows every 40px from y=70 with a 496px share bar.
  - **Feb, Mar and Apr sandbox net is genuinely $0.00**: `Other savings` cancels the usage line for line, so the sandbox ran entirely on the migration credit those months. A share-of-net donut has nothing to divide there, so those periods print a sentence instead ("usage of $X was fully covered by credit") and keep the service table.
  - **Sub-cent residue matters**: GCP exports values like `-0.000179`, and summing them produced an "Other: $-0.01" legend row. `gen.read` and `gen.merge` now round to 2dp and clamp anything under half a cent to zero.
  - **The credit card is today's contract position, not a period-end balance.** It shows the same figure in every view, so the label changed from "Remaining credit balance at period end" to "as at 31 July 2026" in all three views, EN and AR.
- **Export trap (4 Aug 2026, cost a round trip):** a Reports CSV pulled with the credits and discounts options switched off still exports cleanly, with the correct service list and correct `List cost ($)`, but `Subtotal ($)` silently equals `List cost ($)`. Nothing in the file flags it. **Check the `Negotiated savings ($)` column: all zeros means the net figures are unusable.** Majid's Jan to Jun 2026 pull came back this way, reporting $1,174,633 of usage as net when the true net for those six months is $614,875, a $559,758 gap. The discounts cannot be back-derived: the per-service rate is not stable (Chronicle 61.1% across H1 against 69.9% in July; Compute Engine 53.7% against 11.9%; Security Command Center 52.6% against 0%), so apportioning a period total across its months would smear one-off credits into months that never carried them. The fix is a re-export of the same date ranges with the panel untouched after a known-good pull. The parser that reads these CSVs is `scratchpad/finops/period.py`, validated against the published July view ($145,813 net, $318,283 gross, top four services and their shares, to the cent).
- **Credits fact (2 Aug 2026, console):** the $450K migration credit is FULLY CONSUMED (0% remaining, end date 29 Oct 2026); spend now runs at negotiated rates. Only the unused $1K Gen App Builder trial remains (expires 9 Nov 2026). The builder's credits-note field must be refreshed monthly from Billing > Credits.

## What is NOT here

- The claude.ai **MoEnergy project** docs (cloud strategy PDFs, org circular, business case, EA procedures). All facts the artifacts depend on are already baked into the CLAUDE.md files — do not re-derive; ask Majid for a PDF only if truly needed (drop into `docs/source/`).
- Live GCP access. Infra steps run in Majid's Cloud Shell per the Admin Guide runbooks; your job here is the artifact set and its operating logic, not cloud mutations.
